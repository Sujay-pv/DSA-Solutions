You are given an integer n. We reorder the digits in any order (including the original order) such that the leading digit is not zero.

Return true if and only if we can do this so that the resulting number is a power of two.

Solution:
class Solution {
    public boolean reorderedPowerOf2(int n) {
         Set<String> powerSet = new HashSet<>();
        for (int i = 0; i < 31; i++) {          // 2^0 to 2^30
            int pow = 1 << i;
            char[] digits = String.valueOf(pow).toCharArray();
            Arrays.sort(digits);
            powerSet.add(new String(digits));
        }
        
        // Signature of n
        char[] nDigits = String.valueOf(n).toCharArray();
        Arrays.sort(nDigits);
        String nSig = new String(nDigits);
        
        return powerSet.contains(nSig);
        
    }
}
